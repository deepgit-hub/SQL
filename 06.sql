Primary Key on StudentID
NOT NULL on StudentName
-----------------------------------------------------QUERY----------------------------------------------------------------------
CREATE TABLE Student_Details(StudentId INT PRIMARY KEY,StudentName VARCHAR(20) NOT NULL , Department VARCHAR(20), Age INT);
